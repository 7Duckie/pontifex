{
	"name": "pontifex-dev",
	"version": "0.0.0",
	"private": true,
	"description": "Development tooling for the Pontifex plugin: wp-env provides the WordPress + database environment the integration tests run against. Not part of the shipped plugin (see .distignore); the plugin itself has no JavaScript build.",
	"license": "GPL-2.0-or-later",
	"devDependencies": {
		"@wordpress/env": "^11.9.0"
	}
}
