# NOTE: `target-branch` below applies to routine version bumps only. GitHub
# IGNORES it for SECURITY updates, which always open against the DEFAULT branch
# — `main` here. Two such PRs (#209, #210) arrived that way; merging one would
# have put a commit on `main` present in neither `staging` nor `dev`, which the
# promotion model in ADR 0007 exists to prevent. ALWAYS check the base of a
# Dependabot PR before merging, and retarget with `gh pr edit <n> --base dev`.

version: 2
updates:

  - package-ecosystem: "composer"
    directory: "/"
    # Dependency updates enter the promotion model at the bottom tier and
    # flow up through the dev -> staging -> main gates (ADR 0007).
    target-branch: "dev"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "security"
    # Group dev-only and runtime updates separately so they can be
    # reviewed with different urgency. Security updates always come
    # through individually (Dependabot's default for security PRs).
    groups:
      dev-dependencies:
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "ci"

  # npm carries one development dependency only: @wordpress/env, which provides
  # the WordPress + database environment the integration tests run against. It
  # is pinned (package.json + package-lock.json) so CI cannot break on a surprise
  # wp-env release; this keeps it current through the same dev gate.
  - package-ecosystem: "npm"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
